% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_environment_white_balance.R
\name{estimate_environment_white}
\alias{estimate_environment_white}
\title{Estimate Lat-Long Environment White}
\usage{
estimate_environment_white(
  environment_map,
  weighting = c("cosine", "solid_angle"),
  hemisphere = c("upper", "full"),
  environment_colorspace = NULL,
  environment_white = NULL
)
}
\arguments{
\item{environment_map}{Lat-long environment map image, \code{rayimg}, array, or filename.}

\item{weighting}{Default \code{"cosine"}. Weighting method. \code{"cosine"} computes
upward diffuse irradiance; \code{"solid_angle"} computes an unprojected spherical
average.}

\item{hemisphere}{Default \code{"upper"}. Hemisphere to use. \code{"upper"} uses only
rows above the horizon; \code{"full"} uses the full lat-long map.}

\item{environment_colorspace}{Default \code{NULL}. Optional colorspace descriptor
used to tag \code{environment_map} on read.}

\item{environment_white}{Default \code{NULL}. Optional white point used to tag
\code{environment_map} on read.}
}
\value{
Length-3 XYZ white point normalized to \code{Y = 1}. The returned vector
includes \code{rgb}, \code{rgb_ratio}, \code{xy}, approximate \code{cct}, \code{sum_weights},
\code{weighting}, and \code{hemisphere} attributes for diagnostics.
}
\description{
Estimate the scene white produced by a lat-long environment map.
The default weighting matches the irradiance seen by an upward-facing diffuse
surface: \code{cos(theta) * d_omega} over the upper hemisphere.
}
